syntax = "proto3";

package gRPC;

option go_package = "handin4/gRPC/handin4";

service MutualExclusion {
  //Node requests access to Critical Section
  rpc RequestAccess(Requester) returns (Reply);
}

message Reply {
  int64 NodeID = 1;
}

message Requester {
  int64 NodeID = 1;
}